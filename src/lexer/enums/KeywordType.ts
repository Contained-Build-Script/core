export enum KeywordType {
    IF,
    ELSE,
    FOR,
    WHILE,
    FOREACH,
    IN,
    OF,
    SWITCH,
    CASE,
    DEFAULT,
    BREAK,
    CONTINUE,
    RETURN,
    DEFINE,
    THROW,
    TRY,
    CATCH,
    WITH,
    TO,
    CONSTANT,
    DELETE,
    NEW,
    CLASS,
    EXTENDS,
    IMPLEMENTS,
    INTERFACE,
    ENUM,
    PUBLIC,
    PRIVATE,
    PROTECTED,
    STATIC,
    ABSTRACT,
    OVERRIDE
}